import { AuthType } from "@/types/auth";
import axios from "axios";

async function saveTemplateData(
  data: any,
  auth: AuthType,
  socket: any
): Promise<void> {

}

export default saveTemplateData;
